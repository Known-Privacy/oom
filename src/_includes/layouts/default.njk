<!DOCTYPE html>
<html lang="en">
  <head>
    {% if title %}
    <title>{{ title }}</title>
    <meta property="og:title" content="{{ title }}" />
    {% endif %}
    <meta charset="UTF-8"/>
    {% if description %}
    <meta name="description" content="{{description}}"/>
    <meta property="og:description" content="{{description}}" />
    {% endif %}
    {% if ogImage %}
    <meta property="og:image" content="{{ogImage}}" />
    {% endif %}
    <meta rel="canonical" href="{{ page.url | toAbsoluteUrl }}">
    <meta property="og:url" content="{{ page.url | toAbsoluteUrl }}" />
    <meta property="og:type" content="website" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="shortcut icon" type="image/x-icon" href="{{ '/assets/manifest/favicon.ico' }}" />
    <link rel="apple-touch-icon" sizes="180x180" href="{{'/assets/manifest/apple-touch-icon.png' }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ '/assets/manifest/favicon-32x32.png' }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ '/assets/manifest/favicon-16x16.png' }}">
    <link rel="mask-icon" href="{{ '/assets/manifest/safari-pinned-tab.svg' }}" color="#59827d">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#e5e5e5">
    <link rel="stylesheet" href="{{ '/style.css' }}"/>
    {% block head %}{% endblock %}
    <script defer data-domain="opt-out.ai" src="https://plausible.io/js/script.js"></script>
  </head>
  <body class="bg-neutral">
    <div class="flex flex-col min-h-screen">
      {% include "_header.njk" %}

      <div class="container mx-auto">
        <main>
        {% block content %}
          {{ content | safe }}
        {% endblock %}
        </main>
      </div>

      {% include "_footer.njk" %}
    </div>
  </body>
</html>
