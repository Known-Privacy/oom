{
    "scripts":{
       "start":"eleventy --serve & npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/style.css --watch",
       "build":"ELEVENTY_PRODUCTION=true eleventy --pathprefix 'oom' && NODE_ENV=production npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/style.css --minify"
    },
    "devDependencies":{
       "@11ty/eleventy":"^1.0.2",
       "html-minifier":"^4.0.0",
       "tailwindcss":"^3.2.0"
    }
 }